pluginManagement {
    plugins {
        id 'org.springframework.boot' version "2.6.6"
        id 'io.spring.dependency-management' version "1.0.+"
    }
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'vulnerable-service'